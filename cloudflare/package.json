{
  "name": "@nyuchi/platform",
  "version": "1.0.0",
  "description": "🇿🇼 Nyuchi Platform - Main application",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "build": "cd .. && npx turbo run build --filter=@nyuchi/auth --filter=@nyuchi/database --filter=@nyuchi/ubuntu && cd cloudflare && wrangler deploy --dry-run",
    "deploy": "cd .. && npx turbo run build --filter=@nyuchi/auth --filter=@nyuchi/database --filter=@nyuchi/ubuntu && cd cloudflare && wrangler deploy",
    "deploy:production": "cd .. && npx turbo run build --filter=@nyuchi/auth --filter=@nyuchi/database --filter=@nyuchi/ubuntu && cd cloudflare && wrangler deploy --env production",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "nyuchi",
    "platform",
    "cloudflare",
    "workers",
    "hono"
  ],
  "author": "Nyuchi Africa",
  "license": "MIT",
  "dependencies": {
    "@nyuchi/auth": "*",
    "@nyuchi/database": "*",
    "@supabase/supabase-js": "^2.49.1",
    "hono": "^4.6.14",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
